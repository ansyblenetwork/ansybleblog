name: Ansyble's Blog
baseurl: ansybleblog

permalink: /:year/:month/:day/:title
plugins:
  - jekyll-seo-tag
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "page"
